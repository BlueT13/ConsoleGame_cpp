-Leak Check 복습
-null check 복습
-char* 복습
-2차원 배열, 이중포인터 복습
--------------------------------------------------------------------
void Print(const char* _Value)
{
	std::cout << _Value;
}
const char* Text = "AAA";
Print(Text);

코드 75
const 붙여야하는 이유 : 코드영역에 있음 lvalue, rvalue
--------------------------------------